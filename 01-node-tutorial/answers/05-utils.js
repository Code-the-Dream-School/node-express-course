const meetFamily = (husbandName, daughterName) => {
    console.log(`My husband's name is ${husbandName}, my daughter's name is ${daughterName}.`)
}

const meetPets = (cat, fishOne, fishTwo) => {
    console.log(`My cat's name is ${cat}, my fish are ${fishOne} and ${fishTwo}.`)
}

module.exports = {
    meetFamily, 
    meetPets
};